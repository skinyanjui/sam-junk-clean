
// This file is now only kept for compatibility with existing imports.
// Data is now fetched from the database via serviceLocationsService.ts
import { LocationData } from '@/types/locations';

export const serviceLocations: LocationData[] = [];

export default serviceLocations;
